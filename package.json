{
  "name": "email-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Consent-aware bulk email and follow-up automation service",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "lint": "node --check src/server.js && node --check src/app.js && node --check src/db.js && node --check src/email.js && node --check src/importer.js && node --check src/queue.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "busboy": "^1.6.0",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "nodemailer": "^7.0.6",
    "xlsx": "^0.18.5"
  }
}